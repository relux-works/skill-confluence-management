# STORY-260213-3tmtjd: dsl-query-layer

## Description
DSL query layer for agent-facing reads. Copy tokenizer from jira-management (95% reuse). Adapt parser for Confluence operations: get, list, tree, children, ancestors, search, spaces, history. New field presets (minimal/default/overview/full for pages and spaces). DSL executor mapping operations to API calls. Field selector with Confluence field mappings. CLI q command.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
