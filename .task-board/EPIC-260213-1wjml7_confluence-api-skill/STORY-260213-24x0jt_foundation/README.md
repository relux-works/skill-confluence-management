# STORY-260213-24x0jt: foundation

## Description
Project scaffold, config module, auth module, auth/config CLI commands, setup/deinit scripts. Copy & adapt from jira-management. Go mod init, directory structure, Makefile, config at ~/.config/confluence-mgmt/, credentials in OS keychain.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
