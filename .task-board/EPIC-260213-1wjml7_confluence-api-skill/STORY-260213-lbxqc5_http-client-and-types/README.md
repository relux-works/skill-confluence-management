# STORY-260213-lbxqc5: http-client-and-types

## Description
Domain types (Page, Space, Label, Version, Ancestor, SearchResult). HTTP client with v1/v2 dual routing, /wiki prefix, retry logic, rate limit header parsing. Space key-to-ID resolver with caching. Cursor & offset pagination helpers.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
