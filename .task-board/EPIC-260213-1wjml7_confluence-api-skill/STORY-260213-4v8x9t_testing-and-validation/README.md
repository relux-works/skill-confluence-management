# STORY-260213-4v8x9t: testing-and-validation

## Description
Unit tests for all modules (config, auth, HTTP client, domain types, DSL parser/executor, field selector, space resolver). Integration tests against real Confluence (Cloud). E2E smoke tests: agent reads page, creates page, searches, updates. Output format validation (compact, json, text).

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
