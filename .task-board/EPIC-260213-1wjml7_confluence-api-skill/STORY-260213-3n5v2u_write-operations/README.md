# STORY-260213-3n5v2u: write-operations

## Description
Write CLI commands: page create (--space, --title, --body, --parent, --body-file), page update (--title, --body, version increment + 409 retry), label add/remove, grep command (regex search across page titles/bodies). Page move, archive, delete.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
